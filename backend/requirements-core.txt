# Core backend deps for local dev (PDF + image tools except Remove Background).
# Use Python 3.11 or 3.12 — Python 3.14 cannot install rembg/traits yet.
# Full stack (including rembg): pip install -r requirements.txt

Flask>=2.2,<4
flask-cors>=3.0
gunicorn>=20.1
Pillow>=9.0.0
PyMuPDF>=1.22.0
python-docx>=0.8.11
reportlab>=3.6.12
piexif>=1.1.3
Werkzeug>=3.0
